<!--
Thanks for opening a PR. Keep the description tight: reviewers should be
able to skim it in under a minute.

Linked task ID: include `Closes #<issue>` or the task ID from
docs/02-tasks.md (e.g. `E3-9`) so the work shows up in the roadmap view.
-->

## Summary
<!-- 1–3 bullets on WHAT changed and WHY. Skip the obvious. -->
-

## Test plan
<!-- Bulleted checklist of what was verified. Cite commands or test files. -->
- [ ] `make tests` (lint + types + unit + ≥80% coverage)
- [ ]

## Risk / blast radius
<!-- One line. e.g. "scoped to api/, no schema changes" or "touches __main__.py
boot order, validated by tests/e2e/test_local_smoke.py". -->

## Out of scope
<!-- Anything reviewers should NOT expect to find here, with a pointer to the
follow-up. Optional. -->

<!--
ADR / contract changes? Reference the ADR or doc/integration/*.md spec.
Breaking change? Add the `bypass-breaking-change` label and the rationale.
-->
